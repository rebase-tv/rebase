---
import Layout from "@layouts/main.astro"
import Container from "@components/container.astro"
import Logo from "@components/logo.astro"
---

<Layout title="REBASE">
  <main class="flex min-h-full overflow-hidden pt-16 sm:py-28">
    <div class="mx-auto flex w-full max-w-2xl flex-col px-4 sm:px-6">
      <a href="/" aria-label="Home">
        <Logo class="mx-auto h-10 w-auto" />
      </a>
      <div class="relative mt-12 sm:mt-16">
        <svg
          viewBox="0 0 1090 1090"
          aria-hidden="true"
          fill="none"
          preserveAspectRatio="none"
          width="1090"
          height="1090"
          class="absolute -top-7 left-1/2 -z-10 h-[788px] -translate-x-1/2 stroke-gray-300/30 [mask-image:linear-gradient(to_bottom,white_20%,transparent_75%)] sm:-top-9 sm:h-auto"
        >
          <circle cx={545} cy={545} r="544.5"></circle>
          <circle cx={545} cy={545} r="480.5"></circle>
          <circle cx={545} cy={545} r="416.5"></circle>
          <circle cx={545} cy={545} r="352.5"></circle>
        </svg>
        <!-- <h1 -->
        <!--   class="text-center text-2xl font-medium tracking-tight text-gray-900" -->
        <!-- > -->
        <!--   TITLE GOES HERE -->
        <!-- </h1> -->
        <!-- { -->
        <!--   subtitle && ( -->
        <!--     <p class="mt-3 text-center text-lg text-gray-600">{subtitle}</p> -->
        <!--   ) -->
        <!-- } -->
      </div>
      <!-- <div -->
      <!--   class="-mx-4 mt-10 flex-auto py-10 px-4 shadow-2xl shadow-gray-900/10 sm:mx-0 sm:flex-none sm:rounded-5xl sm:p-24" -->
      <!-- > -->
      <img
        src="/images/share.png"
        width={1200}
        height={630}
        class="rounded-2xl shadow-2xl shadow-gray-900"
      />
      <!-- </div> -->
    </div>
  </main>
</Layout>
